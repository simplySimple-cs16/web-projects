{
  "name": "calculator",
  "version": "1.0.0",
  "description": "A simple calculator that runs on all the calculation at server rather than at the browser",
  "main": "calculator.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kadum",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2"
  }
}
